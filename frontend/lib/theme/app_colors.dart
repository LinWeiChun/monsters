import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const legacyBackgroundLight = Color(0xFFFFFED4);
  static const legacyBackgroundSoft = Color(0xFFFFED97);
  static const legacyWarm = Color(0xFFA0522D);
  static const legacyWarmOpacity = Color(0xE6A0522D);

  static const seed = legacyWarm;

  static const lightBackground = legacyBackgroundLight;
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightOutline = Color(0xFFD8B08E);

  static const darkBackground = Color(0xFF21140F);
  static const darkSurface = Color(0xFF2B1B14);
  static const darkOutline = Color(0xFF76513C);
}
